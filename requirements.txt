# =============================================================================
# Python 依赖清单
# - 运行时依赖：按你的项目取消注释 / 添加
# - 开发 / 测试 / 质量工具：CI 会安装本文件全部内容
# =============================================================================

# ===== 运行时依赖（示例，按项目修改）=====
# flask>=3.0
# fastapi>=0.110
# requests>=2.31
# gunicorn>=21.2

# ===== 开发 / 测试 / 代码质量 =====
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4

# ===== 生产部署（systemd 启动需要，二选一，与 server-init.sh 的 EXEC_START 对应）=====
# gunicorn>=22.0   # Flask / WSGI
# uvicorn>=0.30    # FastAPI / ASGI
