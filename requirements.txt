# =============================================================================
# Python 依赖清单
# - 运行时依赖：按你的项目取消注释 / 添加
# - 开发 / 测试 / 质量工具：CI 会安装本文件全部内容
# =============================================================================

# ===== 运行时依赖（示例，按项目修改）=====
flask>=3.1.3
flask-cors>=4.0        # 允许浏览器跨域调用 /chat
openai>=1.50.0         # 调用大模型（DeepSeek / 通义 / 智谱 等 OpenAI 兼容接口）
# fastapi>=0.110
# requests>=2.31
gunicorn>=26.0.0

# ===== 开发 / 测试 / 代码质量 =====
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4

# ===== 如果用 FastAPI 而非 Flask，把上面 gunicorn 换成 =====
# uvicorn>=0.30
